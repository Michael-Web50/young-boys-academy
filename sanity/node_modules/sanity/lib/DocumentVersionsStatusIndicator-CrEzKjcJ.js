import { a as RhombusIcon, n as getVersionFilterLabel, r as useAgentBundles } from "./TimeZoneButton-zt-2o4rU.js";
import { bt as getVariantTitle, nn as useSchema } from "./datastores-QCSg1Xje.js";
import { t as useTranslation } from "./useTranslation-Cqs6Itwv.js";
import { c as useWorkspace } from "./studioClient-BHPpGoe6.js";
import { $ as getTargetDocument, Tt as PUBLISHED, Z as useAllVariants, et as getVariantPublishedSibling, r as sortReleases, rt as usePerspective, t as useActiveReleases, tt as useDocumentVersions, wt as LATEST } from "./useActiveReleases-DdQHoUIU.js";
import { _ as isSystemBundle } from "./draftUtils-D3HJGQaW.js";
import { i as CircleSmallIcon, n as ReleaseAvatarIcon, r as RingIcon, t as ReleaseAvatar } from "./ReleaseAvatar-DGSHma7q.js";
import { n as useRelativeTime } from "./ContextMenuButton--rYZ0QyK.js";
import { g as readVersionType, p as isPublishedVersion } from "./utils-BEonucOH.js";
import { c } from "react/compiler-runtime";
import { jsx, jsxs } from "react/jsx-runtime";
import { Card, Flex, Stack, Text } from "@sanity/ui";
import { styled } from "styled-components";
import { Box as Box$1 } from "ui5";
var titleStack = "_1l3tfji1", updatedAtText = "_1l3tfji2", variantIconCard = "_1l3tfji0", versionStatusItem = "_1l3tfji3";
/**
* Relative-time label for a document version in the versions status tooltip.
*
* Published (non-live-edit) versions always use "Published". Everything else uses "Created"
* when `_createdAt` matches `_updatedAt`, otherwise "Edited".
*
* @internal
*/
function getDocumentVersionStatusTimestampKey(version, liveEdit) {
	return isPublishedVersion(version) && !liveEdit ? "document-status.published-at" : version._createdAt === version._updatedAt ? "document-status.created" : "document-status.edited";
}
/**
* Title line in the document versions status tooltip.
*
* When variants are enabled, the default lane is a real variant, so unvarianted versions are
* labeled "All users (Default) · Published". Without variants, only the perspective
* (Published / Draft / release) is shown.
*
* @internal
*/
function getDocumentVersionStatusTitle({ variantsEnabled, variantTitle, releaseTitle }) {
	return variantsEnabled ? `${variantTitle} · ${releaseTitle}` : releaseTitle;
}
const KIND_ORDER = {
	published: 0,
	drafts: 1,
	release: 2,
	agent: 3
};
function bundleSortLabel(bundle) {
	return bundle.release?.metadata?.title ?? bundle.id;
}
function compareResolvedBundles(left, right, sortedReleases) {
	let kindDelta = KIND_ORDER[left.kind] - KIND_ORDER[right.kind];
	if (kindDelta !== 0) return kindDelta;
	if (left.kind === "release" && right.kind === "release") {
		let leftIndex = sortedReleases.findIndex((release) => release._id === left.id), rightIndex = sortedReleases.findIndex((release) => release._id === right.id), normalizedLeftIndex = leftIndex === -1 ? 2 ** 53 - 1 : leftIndex, normalizedRightIndex = rightIndex === -1 ? 2 ** 53 - 1 : rightIndex;
		if (normalizedLeftIndex !== normalizedRightIndex) return normalizedLeftIndex - normalizedRightIndex;
	}
	return bundleSortLabel(left).localeCompare(bundleSortLabel(right));
}
function resolveVersionBundle(version, releasesById) {
	switch (readVersionType(version)) {
		case "release": {
			let releaseRef = version._system.release?._ref, release = releaseRef ? releasesById.get(releaseRef) : void 0;
			return {
				id: release?._id ?? releaseRef ?? version._id,
				kind: "release",
				release
			};
		}
		case "draft": return {
			id: "drafts",
			kind: "drafts"
		};
		case "agent": return {
			id: version._system.bundleId ?? version._id,
			kind: "agent"
		};
		default: return {
			id: "published",
			kind: "published"
		};
	}
}
function compareDocumentVersionsForStatus(left, right, releasesById, sortedReleases) {
	let leftVariantId = left._system.variant?._ref ?? "", rightVariantId = right._system.variant?._ref ?? "";
	return leftVariantId === rightVariantId ? compareResolvedBundles(resolveVersionBundle(left, releasesById), resolveVersionBundle(right, releasesById), sortedReleases) : leftVariantId ? rightVariantId ? leftVariantId.localeCompare(rightVariantId) : 1 : -1;
}
/**
* Groups document versions by variant (default first, then by variant id) and sorts each group
* published → drafts → releases → agent bundles. Releases follow `sortReleases()`: undecided, then
* scheduled, then ASAP (each by date descending). Releases missing from that list fall back to title.
*
* When `variantsEnabled` is false, versions that belong to a variant are omitted so the tooltip
* only lists default-lane perspectives. Agent bundle versions are omitted unless
* `showAgentVersions` is true (see `SHOW_AGENT_VERSIONS_IN_STATUS`).
*
* @internal
*/
function groupDocumentVersionsForStatus(versions, releasesById, variantsById, { variantsEnabled = !0, showAgentVersions = !1 } = {}) {
	let releases = Array.from(releasesById.values()), sortedReleases = sortReleases(releases), sortedVersions = versions.filter((version) => !(!variantsEnabled && version._system.variant?._ref || !showAgentVersions && readVersionType(version) === "agent")).toSorted((left, right) => compareDocumentVersionsForStatus(left, right, releasesById, sortedReleases)), groups = [];
	for (let version of sortedVersions) {
		let variantId = version._system.variant?._ref, lastGroup = groups.at(-1), item = {
			version,
			variant: variantId ? variantsById.get(variantId) : void 0,
			release: version._system.release?._ref ? releasesById.get(version._system.release._ref) : void 0
		};
		if (lastGroup && lastGroup.variantId === variantId) {
			lastGroup.items.push(item);
			continue;
		}
		groups.push({
			variantId,
			items: [item]
		});
	}
	return groups;
}
/**
* Displays document status for all of the versions of a given document.
*
*
* @internal
*/
function DocumentVersionsStatus(t0) {
	let $ = c(18), { documentGroupId } = t0, { t } = useTranslation(), { byId: releasesById } = useActiveReleases(), { byId: variantsById } = useAllVariants(), t1;
	$[0] === documentGroupId ? t1 = $[1] : (t1 = { documentId: documentGroupId }, $[0] = documentGroupId, $[1] = t1);
	let { loading, versions } = useDocumentVersions(t1), variantsEnabled = !!useWorkspace().beta?.variants?.enabled, t2;
	$[2] !== releasesById || $[3] !== variantsById || $[4] !== variantsEnabled || $[5] !== versions ? (t2 = groupDocumentVersionsForStatus(versions, releasesById, variantsById, {
		variantsEnabled,
		showAgentVersions: !1
	}), $[2] = releasesById, $[3] = variantsById, $[4] = variantsEnabled, $[5] = versions, $[6] = t2) : t2 = $[6];
	let versionGroups = t2;
	if (loading) {
		let t3;
		$[7] === t ? t3 = $[8] : (t3 = t("common.loading"), $[7] = t, $[8] = t3);
		let t4;
		return $[9] === t3 ? t4 = $[10] : (t4 = /* @__PURE__ */ jsx(Box$1, {
			padding: 2,
			children: /* @__PURE__ */ jsx(Text, {
				muted: !0,
				size: 1,
				children: t3
			})
		}), $[9] = t3, $[10] = t4), t4;
	}
	let t3;
	if ($[11] !== variantsEnabled || $[12] !== versionGroups) {
		let t4;
		$[14] === variantsEnabled ? t4 = $[15] : (t4 = (group, groupIndex, allGroups) => /* @__PURE__ */ jsx(Card, {
			borderBottom: groupIndex < allGroups.length - 1,
			paddingBottom: +(groupIndex < allGroups.length - 1),
			children: group.items.map((item) => /* @__PURE__ */ jsx(VersionStatus, {
				variantsEnabled,
				...item
			}, item.version._id))
		}, group.variantId ?? "default"), $[14] = variantsEnabled, $[15] = t4), t3 = versionGroups.map(t4), $[11] = variantsEnabled, $[12] = versionGroups, $[13] = t3;
	} else t3 = $[13];
	let t4;
	return $[16] === t3 ? t4 = $[17] : (t4 = /* @__PURE__ */ jsx(Stack, {
		gap: 1,
		children: t3
	}), $[16] = t3, $[17] = t4), t4;
}
function VersionStatus(t0) {
	let $ = c(38), { release, version, variant, variantsEnabled } = t0, { t } = useTranslation(), schema = useSchema(), { bundles } = useAgentBundles(), t1;
	$[0] !== schema || $[1] !== version._type ? (t1 = version._type && schema.get(version._type)?.liveEdit, $[0] = schema, $[1] = version._type, $[2] = t1) : t1 = $[2];
	let liveEdit = !!t1, fullTitle, isTruncated, releasePerspective, t2;
	if ($[3] !== bundles || $[4] !== release || $[5] !== t || $[6] !== variant || $[7] !== variantsEnabled || $[8] !== version) {
		let variantTitle = variant ? getVariantTitle(variant) : t("document-group.base-variant");
		releasePerspective = getReleasePerspective({
			release,
			version
		});
		let { displayTitle: releaseTitle, fullTitle: t3, isTruncated: t4 } = getVersionFilterLabel(releasePerspective, t, bundles);
		fullTitle = t3, isTruncated = t4, t2 = getDocumentVersionStatusTitle({
			variantsEnabled,
			variantTitle,
			releaseTitle
		}), $[3] = bundles, $[4] = release, $[5] = t, $[6] = variant, $[7] = variantsEnabled, $[8] = version, $[9] = fullTitle, $[10] = isTruncated, $[11] = releasePerspective, $[12] = t2;
	} else fullTitle = $[9], isTruncated = $[10], releasePerspective = $[11], t2 = $[12];
	let title = t2, t3;
	$[13] === Symbol.for("react.memo_cache_sentinel") ? (t3 = {
		minimal: !0,
		useTemporalPhrase: !0
	}, $[13] = t3) : t3 = $[13];
	let updatedAt = useRelativeTime(version._updatedAt, t3), t4;
	$[14] !== liveEdit || $[15] !== t || $[16] !== updatedAt || $[17] !== version ? (t4 = t(getDocumentVersionStatusTimestampKey(version, liveEdit), { date: updatedAt }), $[14] = liveEdit, $[15] = t, $[16] = updatedAt, $[17] = version, $[18] = t4) : t4 = $[18];
	let timestampLabel = t4, t5 = isTruncated ? fullTitle : void 0, t6;
	$[19] !== t5 || $[20] !== title ? (t6 = /* @__PURE__ */ jsx(Text, {
		size: 1,
		title: t5,
		weight: "medium",
		children: title
	}), $[19] = t5, $[20] = title, $[21] = t6) : t6 = $[21];
	let t7;
	$[22] === timestampLabel ? t7 = $[23] : (t7 = /* @__PURE__ */ jsx(Text, {
		className: updatedAtText,
		muted: !0,
		size: 1,
		children: timestampLabel
	}), $[22] = timestampLabel, $[23] = t7);
	let t8;
	$[24] !== t6 || $[25] !== t7 ? (t8 = /* @__PURE__ */ jsxs(Stack, {
		className: titleStack,
		gap: 2,
		children: [t6, t7]
	}), $[24] = t6, $[25] = t7, $[26] = t8) : t8 = $[26];
	let t9;
	$[27] !== variant || $[28] !== variantsEnabled ? (t9 = variantsEnabled && variant ? /* @__PURE__ */ jsx(Card, {
		className: variantIconCard,
		tone: "suggest",
		children: /* @__PURE__ */ jsx(Text, {
			size: 2,
			children: /* @__PURE__ */ jsx(RhombusIcon, {})
		})
	}) : null, $[27] = variant, $[28] = variantsEnabled, $[29] = t9) : t9 = $[29];
	let t10;
	$[30] === releasePerspective ? t10 = $[31] : (t10 = /* @__PURE__ */ jsx(ReleaseAvatar, {
		fontSize: 2,
		size: "small",
		release: releasePerspective,
		padding: 0
	}), $[30] = releasePerspective, $[31] = t10);
	let t11;
	$[32] !== t10 || $[33] !== t9 ? (t11 = /* @__PURE__ */ jsxs(Flex, {
		align: "center",
		flex: "none",
		gap: 1,
		paddingRight: 2,
		children: [t9, t10]
	}), $[32] = t10, $[33] = t9, $[34] = t11) : t11 = $[34];
	let t12;
	return $[35] !== t11 || $[36] !== t8 ? (t12 = /* @__PURE__ */ jsx(Box$1, {
		className: versionStatusItem,
		children: /* @__PURE__ */ jsxs(Flex, {
			gap: 2,
			justify: "space-between",
			paddingY: 2,
			children: [t8, t11]
		})
	}), $[35] = t11, $[36] = t8, $[37] = t12) : t12 = $[37], t12;
}
function getReleasePerspective({ release, version }) {
	switch (readVersionType(version)) {
		case "draft": return LATEST;
		case "release": return release ?? version._system.bundleId ?? "published";
		case "agent": return version._system.bundleId ?? "published";
		default: return PUBLISHED;
	}
}
const DOCUMENT_STATUS_ICONS_BY_OUTCOME = {
	variantPublishedWithDraft: [
		"variant",
		"draft",
		"published"
	],
	variantPublished: ["variant", "published"],
	variantDraftOnly: ["variant", "draft"],
	defaultPublishedWithDraft: ["draft", "published"],
	defaultPublished: ["published"],
	defaultUnpublished: [],
	inReleaseWithVariant: ["variant", "release"],
	inRelease: ["release"],
	notInRelease: []
};
function resolveSystemVariantOutcome(context) {
	let { variantId, documentVersions } = context;
	if (!variantId) return resolveSystemDefaultOutcome(context);
	let draft = getTargetDocument({
		bundle: "drafts",
		variant: variantId,
		documentVersions
	}), published = getVariantPublishedSibling({
		variant: variantId,
		documentVersions
	});
	return !draft && !published ? resolveSystemDefaultOutcome(context) : published && draft ? "variantPublishedWithDraft" : published ? "variantPublished" : "variantDraftOnly";
}
function resolveSystemDefaultOutcome(context) {
	let { documentVersions } = context, published = !!getTargetDocument({
		bundle: "published",
		variant: void 0,
		documentVersions
	}), draft = published && !!getTargetDocument({
		bundle: "drafts",
		variant: void 0,
		documentVersions
	});
	return published && draft ? "defaultPublishedWithDraft" : published ? "defaultPublished" : "defaultUnpublished";
}
function resolveMembershipVariantOutcome(context) {
	let { bundle, variantId, documentVersions } = context;
	return variantId && getTargetDocument({
		bundle,
		variant: variantId,
		documentVersions
	}) ? "inReleaseWithVariant" : resolveMembershipDefaultOutcome(context);
}
function resolveMembershipDefaultOutcome(context) {
	let { bundle, documentVersions } = context;
	return getTargetDocument({
		bundle,
		variant: void 0,
		documentVersions
	}) ? "inRelease" : "notInRelease";
}
/**
* Resolves the named outcome for the selected perspective and variant.
*
* @internal
*/
function resolveDocumentStatusIconsOutcome(context) {
	return isSystemBundle(context.bundle) ? resolveSystemVariantOutcome(context) : resolveMembershipVariantOutcome(context);
}
/**
* Resolves which document status icons to show for the selected perspective and variant.
*
* @internal
*/
function resolveDocumentStatusIcons(context) {
	return DOCUMENT_STATUS_ICONS_BY_OUTCOME[resolveDocumentStatusIconsOutcome(context)];
}
const IconSlotRoot = styled.div.withConfig({
	displayName: "IconSlotRoot",
	componentId: "sc-otll23-0"
})`display:flex;align-items:center;justify-content:center;width:15px;flex-shrink:0;&[data-status='published']{--card-icon-color:var(--card-badge-positive-dot-color);}&[data-status='draft']{--card-icon-color:var(--card-badge-caution-dot-color);}&[data-status='variant']{--card-icon-color:var(--card-badge-suggest-dot-color);}`;
/**
* The icons are `1em` boxes in a 25-unit viewBox, like everything in `@sanity/icons`, so a `Text` is
* what sizes them: it resolves the em to the text size's icon size — 17px at size 0 — and applies
* the negative margin that centres the glyph on the cap height. It is also what makes them honour
* `--card-icon-color`, which is how each icon gets its tone.
*/
function IconSlot(t0) {
	let $ = c(5), { status, children } = t0, t1;
	$[0] === children ? t1 = $[1] : (t1 = /* @__PURE__ */ jsx(Text, {
		size: 2,
		children
	}), $[0] = children, $[1] = t1);
	let t2;
	return $[2] !== status || $[3] !== t1 ? (t2 = /* @__PURE__ */ jsx(IconSlotRoot, {
		"data-status": status,
		children: t1
	}), $[2] = status, $[3] = t1, $[4] = t2) : t2 = $[4], t2;
}
function VariantIcon() {
	let $ = c(1), t0;
	return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = /* @__PURE__ */ jsx(IconSlot, {
		status: "variant",
		children: /* @__PURE__ */ jsx(RhombusIcon, {})
	}), $[0] = t0) : t0 = $[0], t0;
}
function renderDocumentStatusIcon(icon, selectedPerspective) {
	switch (icon) {
		case "variant": return /* @__PURE__ */ jsx(VariantIcon, {}, "variant");
		case "release": return /* @__PURE__ */ jsx(IconSlot, { children: /* @__PURE__ */ jsx(ReleaseAvatarIcon, {
			release: selectedPerspective,
			size: "small"
		}) }, "release");
		case "draft": return /* @__PURE__ */ jsx(IconSlot, {
			status: "draft",
			children: /* @__PURE__ */ jsx(RingIcon, {})
		}, "draft");
		case "published": return /* @__PURE__ */ jsx(IconSlot, {
			status: "published",
			children: /* @__PURE__ */ jsx(CircleSmallIcon, {})
		}, "published");
	}
}
/**
* Renders icons describing the document's status in the selected perspective and variant. The
* perspective decides what the icons describe, and the selected variant then narrows it. Icons
* appear in a fixed order: the rhombus (variant), the release icon, the yellow draft ring, then the
* green published disc. At most three render at once.
*
* See `resolveDocumentStatusIcons.ts` for the full decision logic.
*
* @internal
*/
function DocumentVersionsStatusIndicator(t0) {
	let $ = c(14), { documentVersions } = t0, { bundle, selectedPerspective, selectedVariant } = usePerspective(), T0, t1, t2, t3;
	if ($[0] !== bundle || $[1] !== documentVersions || $[2] !== selectedPerspective || $[3] !== selectedVariant?._id) {
		t3 = Symbol.for("react.early_return_sentinel");
		bb0: {
			let icons = resolveDocumentStatusIcons({
				bundle,
				variantId: selectedVariant?._id,
				documentVersions
			});
			if (icons.length === 0) {
				t3 = null;
				break bb0;
			}
			T0 = Flex, t1 = "center";
			let t4;
			$[8] === selectedPerspective ? t4 = $[9] : (t4 = (icon) => renderDocumentStatusIcon(icon, selectedPerspective), $[8] = selectedPerspective, $[9] = t4), t2 = icons.map(t4);
		}
		$[0] = bundle, $[1] = documentVersions, $[2] = selectedPerspective, $[3] = selectedVariant?._id, $[4] = T0, $[5] = t1, $[6] = t2, $[7] = t3;
	} else T0 = $[4], t1 = $[5], t2 = $[6], t3 = $[7];
	if (t3 !== Symbol.for("react.early_return_sentinel")) return t3;
	let t4;
	return $[10] !== T0 || $[11] !== t1 || $[12] !== t2 ? (t4 = /* @__PURE__ */ jsx(T0, {
		align: t1,
		children: t2
	}), $[10] = T0, $[11] = t1, $[12] = t2, $[13] = t4) : t4 = $[13], t4;
}
export { DocumentVersionsStatus as n, DocumentVersionsStatusIndicator as t };

//# sourceMappingURL=DocumentVersionsStatusIndicator-CrEzKjcJ.js.map
import { $ as ResolvedInterfacesOf, A as ApplicationId, B as getDeployedConfigModuleRef, C as findDeployedConfigRef, D as parseInstalledApplication, E as InstalledApplication, F as brandApplicationId, G as brandCanvasId, H as Canvas, I as APP_TYPE_SLUG, J as AbstractApplicationType, K as parseCanvas, L as ActiveConfig, M as BrettWorkspace, N as ExternalApplication, O as ActiveDeployment, P as InternalApplication, Q as ResolvedInterfaceOfType, R as CONFIG_MODULE_EXPOSE, S as MediaLibraryApplication, T as InstallationId, U as CanvasApplication, V as RemoteModule, W as CanvasId, X as LocalApplication, Y as IconDescriptor, Z as ResolvedInterface, _ as CoreApplication, a as Project, at as LocalInterface, b as LocalCoreApplicationManifest, c as brandProjectId, ct as PanelComponent, d as InternalStudioApplication, dt as Organization, et as DockGroup, f as LocalStudioApplicationManifest, ft as OrganizationId, g as CoreApp, h as parseStudioApplication, ht as parseOrganization, i as StudioWorkspace, it as InterfacesOf, j as BrettApplication, k as ApplicationBase, l as parseProject, lt as TileSize, m as Workspace, mt as brandOrganizationId, n as ApplicationList, nt as InterfaceOfType, o as ProjectId, ot as LocalInterfaceSchema, p as Studio, pt as OrganizationMember, q as AbstractApplication, r as StudioApplication, rt as InterfaceSchema, s as ProjectMember, st as Panel, t as Application, tt as Interface, u as ExternalStudioApplication, ut as getInitials, v as ExternalCoreApplication, w as Installation, x as parseCoreApplication, y as InternalCoreApplication, z as LocalAppConfig } from "./application-list-1MWg2hXK.js";
import { n as RemoteModuleRenderOptions, r as ResolvedConfig, t as Config } from "./config-DQiDQCp6.js";
import { a as getSanityEnv, c as parseLocalApplications, i as getSanityDomain, n as getApiHost, o as createCoreApplicationFromLocal, r as getApplicationDomain, s as createStudioApplicationFromLocal, t as isSanityClientError } from "./index-C1PitWMB.js";
/**
 * Log levels in order of verbosity (least to most)
 * - none: Silent
 * - error: Critical failures that prevent operation
 * - warn: Issues that may cause problems but don't stop execution
 * - info: High-level informational messages (default)
 * - debug: Detailed debugging information (maintainer level)
 * - trace: Very detailed tracing — sets `internal: true` on context
 * @public
 */
type LogLevel = "none" | "error" | "warn" | "info" | "debug";
/**
 * Namespaces organize logs by functional domain.
 * @internal
 */
type LogNamespace = string;
type LogContext = {
  [key: string]: unknown;
};
/**
 * @public
 */
interface Logger {
  error: (message: string, context?: LogContext) => void;
  warn: (message: string, context?: LogContext) => void;
  info: (message: string, context?: LogContext) => void;
  debug: (message: string, context?: LogContext) => void;
  child: (domain: string, context?: LogContext) => Logger;
}
interface LoggerOptions {
  namespace?: LogNamespace;
  context?: LogContext;
  logLevel?: LogLevel;
}
/**
 * Creates a leveled logger with an optional namespace prefix and bound
 * context. Calls below the configured `logLevel` are suppressed; `"none"`
 * silences the logger entirely.
 *
 * Use {@link Logger.child} to derive a sub-logger with an extended namespace
 * (e.g. `parent:domain`) that inherits the parent's level and merges its
 * bound context.
 *
 * @param options - Logger configuration.
 * @param options.namespace - Prepended to every message in `[brackets]`.
 * @param options.context - Bound context merged with per-call context.
 *   Per-call keys win on conflict.
 * @param options.logLevel - Maximum verbosity to emit. Default `"info"`.
 *
 * @example
 * ```ts
 * const log = createLogger({ namespace: "checkout", logLevel: "debug" });
 * log.info("placed", { orderId: "abc" });
 * // → [checkout] placed { orderId: "abc" }
 *
 * const auth = log.child("auth", { tenant: "acme" });
 * auth.warn("token expiring");
 * // → [checkout:auth] token expiring { tenant: "acme" }
 * ```
 *
 * @public
 */
declare function createLogger({ namespace, context: baseContext, logLevel }?: LoggerOptions): Logger;
/**
 * Shared workbench logger instance. Use this from both the workbench host
 * and its remotes so lifecycle and diagnostic logs appear under a single
 * namespace.
 *
 * @public
 */
declare const logger: Logger;
/**
 * Joins multiple path segments into a single path string.
 * Handles null, undefined, and URL objects gracefully.
 *
 * @public
 * @param paths - An array of path segments to join.
 * @returns A single joined path string.
 */
declare const joinUrlPaths: (...paths: Array<string | URL | null | undefined>) => string;
export { APP_TYPE_SLUG, AbstractApplication, AbstractApplicationType, ActiveConfig, ActiveDeployment, Application, ApplicationBase, ApplicationId, ApplicationList, BrettApplication, BrettWorkspace, CONFIG_MODULE_EXPOSE, Canvas, CanvasApplication, CanvasId, type Config, CoreApp, CoreApplication, type DockGroup, ExternalApplication, ExternalCoreApplication, ExternalStudioApplication, IconDescriptor, Installation, InstallationId, InstalledApplication, type Interface, type InterfaceOfType, InterfaceSchema, type InterfacesOf, InternalApplication, InternalCoreApplication, InternalStudioApplication, LocalAppConfig, LocalApplication, LocalCoreApplicationManifest, type LocalInterface, LocalInterfaceSchema, LocalStudioApplicationManifest, LogLevel, LogNamespace, Logger, MediaLibraryApplication, Organization, OrganizationId, OrganizationMember, type Panel, type PanelComponent, Project, ProjectId, ProjectMember, RemoteModule, type RemoteModuleRenderOptions, type ResolvedConfig, ResolvedInterface, ResolvedInterfaceOfType, ResolvedInterfacesOf, Studio, StudioApplication, StudioWorkspace, type TileSize, Workspace, brandApplicationId, brandCanvasId, brandOrganizationId, brandProjectId, createCoreApplicationFromLocal, createLogger, createStudioApplicationFromLocal, findDeployedConfigRef, getApiHost, getApplicationDomain, getDeployedConfigModuleRef, getInitials, getSanityDomain, getSanityEnv, isSanityClientError, joinUrlPaths, logger, parseCanvas, parseCoreApplication, parseInstalledApplication, parseLocalApplications, parseOrganization, parseProject, parseStudioApplication };
//# sourceMappingURL=core.d.ts.map
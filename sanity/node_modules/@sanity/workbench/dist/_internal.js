import { r as logger } from "./defineProperty-CIr4gfLZ.js";
import { n as defineStateTopics, r as disconnectApp, t as connect } from "./bus-DkFosOcj.js";
import { C as getSanityEnv, r as parseLocalApplications } from "./local-apps-C-MhPNYJ.js";
import { n as log, t as createInstance } from "./module-federation-qgks9m28.js";
import { BehaviorSubject } from "rxjs";
const REMOTE_NAME = "workbench-remote", LOCAL_APPS_HMR_EVENT = "sanity:workbench:local-applications";
/**
* Creates a Module Federation instance, loads a remote workbench
* application, and renders it into the provided root element.
*
* @param rootElement - The DOM element to render into
* @param config - Workbench configuration, forwarded to the remote. The CLI
*   sources `organizationId` from `unstable_defineApp` (required there).
* @param options - Rendering options forwarded to the remote
* @returns A cleanup function that unmounts the remote application
*
* @public
*/
async function renderWorkbench(rootElement, config, options) {
	if (!rootElement) throw Error("Missing root element to mount application into");
	let deployedWorkbenchUrl = getSanityEnv() === "staging" ? "workbench-apps-of5p8qpku.run.sanity.work" : "workbench-apps-oSyH1iET5.sanity.run", remoteUrl = import.meta.env.SANITY_INTERNAL_WORKBENCH_REMOTE_URL ?? `https://${deployedWorkbenchUrl}`, remoteModule = await createInstance({
		name: "sanity-workbench",
		plugins: [log(logger.debug)],
		remotes: [{
			name: REMOTE_NAME,
			entry: remoteUrl
		}]
	}).loadRemote(`${REMOTE_NAME}/App`);
	if (!remoteModule || typeof remoteModule.render != "function") throw Error(`Remote module "${REMOTE_NAME}/App" did not expose a render function`);
	let localApplications, appConfigs, cleanupHmr = () => {};
	if (import.meta.hot) {
		let localApps$ = new BehaviorSubject([]), configs$ = new BehaviorSubject([]), handler = (payload) => {
			localApps$.next(parseLocalApplications(payload.applications)), configs$.next(payload.configs ?? []);
		};
		import.meta.hot.on(LOCAL_APPS_HMR_EVENT, handler), import.meta.hot.send("sanity:workbench:get-local-applications"), localApplications = localApps$, appConfigs = configs$, cleanupHmr = () => import.meta.hot?.off(LOCAL_APPS_HMR_EVENT, handler);
	}
	let unmount = remoteModule.render(rootElement, {
		config,
		appConfigs,
		localApplications
	}, options);
	return () => {
		cleanupHmr(), unmount();
	};
}
export { connect, createInstance, defineStateTopics, disconnectApp, renderWorkbench };

//# sourceMappingURL=_internal.js.map